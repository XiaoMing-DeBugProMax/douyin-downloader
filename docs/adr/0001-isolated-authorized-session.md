# 授权会话通过应用隔离登录建立

账号相关功能采用用户主动开启的授权模式，并只通过应用管理的隔离登录环境建立会话；应用不读取或导入用户现有 Edge、Chrome 等浏览器的 Cookie。隔离浏览器环境自行管理会话数据，授权访问在该环境中执行；应用不导出或读取 Cookie 值，只接收筛选后的能力结果。这样可以支持明确授权的数据访问，同时避免把用户日常浏览器会话变成应用的隐式凭据来源，并让退出、清除和失效处理保持在应用自己的安全边界内。
