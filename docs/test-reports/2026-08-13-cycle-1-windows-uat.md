# 周期 1 Windows 验收报告

- 日期：2026-08-13
- 环境：Windows 11 x64、Python 3.12.10、Microsoft Edge
- 验收产物：`dist/抖音视频下载.exe`
- EXE SHA-256：`C974A815EF19CF7D223FABD4E92BB0443797E72E6A44312D585C258D25C67D59`
- 公开样例作品 ID：`7429378937383308594`
- 样例视频 SHA-256：`B594D1DF250D2450266C1D4714BE9C300FEFFCFEB18E1E6B2928823AEFC02093`

本报告不记录分享 URL、Cookie、Authorization、启动令牌、管理令牌或媒体 URL。原始证据位于 Git 忽略的 `build/` 目录。

## 自动化与构建门禁

| 检查 | 结果 |
| --- | --- |
| pytest | PASS：352 passed，2 个仅在未指定构建产物时跳过 |
| Ruff | PASS |
| mypy strict | PASS：32 个源码文件 |
| 仓库与最终 EXE 敏感扫描 | PASS：findings=0 |
| FFmpeg/ffprobe 审计与许可证 | PASS |
| PyInstaller 单文件构建 | PASS |
| 显式指定 EXE 的资源与运行冒烟 | PASS：2 passed |
| Windows 托盘与回收站专项测试 | PASS：5 passed |

## 真实单视频与打包 GUI

| 旅程 | 结果 | 安全证据摘要 |
| --- | --- | --- |
| 实时解析 | PASS | 作品 ID、作者、文案、15279 ms、3 个媒体候选均与基线一致 |
| 实时下载 | PASS | MP4 1,345,693 bytes；SHA-256 与基线一致 |
| 打包 EXE 启动 | PASS | 冷启动控制窗口约 5.7 秒出现；服务只绑定 `127.0.0.1` |
| 连续解析与封面 | PASS | 打包页面连续解析 3 次；封面成功解码 |
| 默认/自定义下载 | PASS | 两份 MP4 均与基线哈希一致，Edge 解码时长约 15.279 秒 |
| 主题、单实例、正常退出 | PASS | 三主题及持久化、重复启动退出 0、端口/互斥锁/runtime/one-file 临时目录释放 |
| 无水印人工抽检 | PASS | 开头、中段、结尾三帧未见平台角标、作者浮水印或其他平台叠加水印 |

## 真实托管归档与状态旅程

| 旅程 | 结果 | 安全证据摘要 |
| --- | --- | --- |
| 默认成果与可选成果 | PASS | 视频、封面、音频、文案、元数据共 5 类成果 |
| 完整性 | PASS | 所有成果大小与 SHA-256 和登记值一致；路径均为相对路径 |
| 播放与探测 | PASS | 视频与 M4A 均通过 ffprobe；封面可解码 |
| 过滤元数据 | PASS | schema v1、作品身份正确、无分享输入或签名媒体 URL |
| 任务与档案库 | PASS | 任务持久化、档案登记、重启后仍可查询 |
| 首次归档根选择 | PASS | 打包应用原生目录对话框选中隔离空目录；设置页回填且本地设置 API 返回相同目录 |
| 暂停与取消 | PASS | 打包 EXE 的真实下载任务被观察为 running，并进入 paused、cancelled |
| 中断、重启、手动继续 | PASS | interrupted 跨进程持久化；重启后显式 resume 成功；无 `.part` 或恢复元数据残留 |
| 打开文件夹 | PASS | 打包应用打开已登记作品目录 |
| 位置不可用与恢复 | PASS | 隔离根断开后显示 `location_unavailable`，原位恢复后重新为 `archived` |
| 数据库恢复 | PASS | 损坏库被隔离；已校验备份恢复成功；重启后档案仍可用 |
| Windows 回收站 | PASS | 作品目录移入回收站后记录删除；授权根保留 |
| 托盘 | PASS | 真实 Windows 托盘图标显示与移除；四项动作接线由集成测试覆盖 |

## 安全复核

- 最终 EXE 的 PyInstaller 目录表包含前端静态资源、应用图标、FFmpeg、ffprobe 和许可证材料，不包含禁入测试配置或凭据文件。
- 44 个隔离应用日志、SQLite、备份、元数据、文本与媒体成果通过项目正式敏感规则扫描，findings=0。
- 所有临时打包进程均已退出；stale `runtime.json` 已从 Git 忽略的验收目录清理。
- Host、会话、同源和管理令牌保护继续由完整测试门禁覆盖；真实旅程只使用运行时签发的本地会话，不读取浏览器 Cookie。

## 人工确认

- Windows 原生“选择本地归档目录”已由用户在隔离打包应用中选择空测试目录；随后通过本地设置 API 核对回填与持久化值完全一致。
- 人工确认后的隔离实例已正常关闭；进程退出、`runtime.json` 删除且端口可重新绑定。
